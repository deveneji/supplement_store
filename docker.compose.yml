version: '3.8'

services:
  # PostgreSQL Database - Render will handle this automatically
  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=supplement_db
      - POSTGRES_USER=supplement_user
      - POSTGRES_PASSWORD=${DB_PASSWORD}  # Use environment variable
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U supplement_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API
  backend:
    build: ./backend
    environment:
      - DEBUG=False
      - DB_NAME=supplement_db
      - DB_USER=supplement_user
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=db
      - DB_PORT=5432
      - SECRET_KEY=${SECRET_KEY}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - backend_media:/app/media

  # Frontend
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  backend_media: